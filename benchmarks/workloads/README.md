# Benchmark workloads

Add small, reproducible workload definitions as benchmark stages are implemented.
Reuse the same workloads when comparing the shared runtime with vLLM.

Record prompt data or its generation seed, model/tokenizer revision, input and
output token counts, sampling settings, batch size, concurrency, and arrival
pattern where applicable. Use synthetic or public prompts, without credentials
or private user data.
